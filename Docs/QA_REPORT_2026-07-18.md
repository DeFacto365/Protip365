# QA execution report — ProTip365 V4 MVP (2026-07-18)

Source: testing agent phase 2 run against `app/` (post RFP-214). Plan: `Docs/QA_TEST_PLAN_V4.md`.

## Build verification (independently run)

- `npx tsc --noEmit`: clean, exit 0 (before and after new tests).
- `npx jest`: 3 suites / 55 tests pass (coding agent's suite, confirmed) → **5 suites / 87 tests pass** after QA added 32 tests (`src/domain/__tests__/qa.extended.test.ts`, `qa.i18nParity.test.ts`).

## Verdict rollup (75 plan scenarios)

25 PASS-unit · ~18 PASS-code-inspection · 14 PARTIAL · 4 FAIL (TP-009 latent, TP-018/019, TP-039, TP-070) · 5 BLOCKED-need-emulator · remainder N/A (deferred/by-design). Full per-TP table lives in the testing agent transcript; defects below are the actionable output.

## Defects (DEF-01 … DEF-15)

| ID | Sev | TP | Summary | Expected vs actual | File |
|---|---|---|---|---|---|
| DEF-01 | High | TP-018/019 | No cross-employer overlap detection | PRD §9: visible non-blocking overlap warning; actual: overlapping same-day shifts save silently | `app/shift-form.tsx`, `app/(tabs)/index.tsx` |
| DEF-02 | High | TP-039 | Mark-not-worked lacks reason codes and `cancelled` | PRD: reason codes (sick, employer_cancelled→`cancelled`, …), `other`+note; actual: free text, always `missed` | `app/complete/[id].tsx` |
| DEF-03 | Med | TP-067 | CSV export missing break details, other_income, deduction_rate columns | 27 cols otherwise correct, escaping OK | `app/(tabs)/settings.tsx:57-85` |
| DEF-04 | Med | TP-070/074 | Money hardcoded `$x.xx` en-format | PRD §14 requires `Intl.NumberFormat`; fr-CA should read `123,45 $` | `src/ui/components.tsx:16-19` |
| DEF-05 | Med | TP-027 | Completion can only toggle breaks taken/skip | PRD §9: adjust break start/duration or add a break at completion | `app/complete/[id].tsx` |
| DEF-06 | Med | TP-016 | end&lt;start silently becomes overnight | Needs explicit "+1 day" cue (or rejection) | `shift-form.tsx:79`, `complete/[id].tsx:82` |
| DEF-07 | Med | TP-066 | Deduction rate not editable per shift at completion | PRD §9: prefolded but editable snapshot | `app/complete/[id].tsx:120,171` |
| DEF-08 | Med | TP-003/009 | Employer lacks default-rate field; no employer edit/archive UI; `remove()` cascade hard-deletes history (currently unreachable from UI) | PRD: never hard-delete employer with history | `src/domain/types.ts`, `src/data/repositories.ts:124-131` |
| DEF-09 | Low | TP-073 | 2 hardcoded English a11y labels (week nav) | Localize | `app/(tabs)/index.tsx:117,133` |
| DEF-10 | Low | TP-058 | `roundCents(1.005)` → 1.00 (float artifact; expect half-up 1.01) | Max 1-cent error | `src/domain/calc.ts:9-11` |
| DEF-11 | Low | TP-008 | Inline add-employer with empty name fails silently | Needs inline error | `shift-form.tsx:108-124` |
| DEF-12 | Low | TP-042 | `disputed` payout status unreachable in UI | Manual mark-disputed control missing | `complete/[id].tsx` |
| DEF-13 | Low | TP-024 | Double-tap Save can duplicate a shift | Disable-while-saving / idempotency guard | `shift-form.tsx:168-183` |
| DEF-14 | Low | TP-050 | Deduction stored as float fraction, not basis points per PRD §10 | Storage refactor | `settingsStore.ts`, `db.ts:17` |
| DEF-15 | Low | a11y | Chip minHeight 40 &lt; 48 dp Android target | Bump touch target | `components.tsx:236-244` |

## Linear tracking

All 15 defects filed 2026-07-18 as RFP-218…RFP-232 (DEF-01→RFP-218 … DEF-15→RFP-232, in order) in the Protip365 project; tracker RFP-216 closed with the full mapping.

## Gap classifications (not defects)

- Text date/time inputs (no Material pickers): acceptable MVP gap; must-fix before beta (PRD §15).
- No device-language auto-detect (defaults en): acceptable MVP gap; required before Phase I release (PRD §14).
- No onboarding screen: first employer is created inline in Add Shift; TP-075 script steps 1–3/20 need adjustment.
- Draft recovery + soft-delete undo: formally deferred (owner ruling 2026-07-18).

## Emulator round

Blocked pending ARM64 emulator fix (host is Windows-on-ARM; see RFP-217).

## Fix verification (2026-07-18)

Independent re-run by the testing agent after the coding agent's DEF-01…DEF-15 fix round.

### Build verification

- `npx tsc --noEmit`: clean, exit 0.
- `npx jest`: **6 suites / 98 tests, all pass** (was 5/87). Delta: +10 tests in new
  `src/domain/__tests__/overlap.test.ts`, +1 in `qa.extended.test.ts` (TP-058 test
  updated to assert half-up `roundCents(1.005) === 1.01`, which now passes).
- i18n parity (`qa.i18nParity.test.ts`) passes against the updated locales: all new
  keys (`shiftForm.overnight`, `shiftForm.overlapWarning`,
  `shiftForm.errors.employerNameRequired`, `schedule.prevWeek/nextWeek/overlap`,
  `complete.addBreak/markDisputed/deductionRate/reasonNote`, `complete.reasons.*`)
  exist in en, fr-CA, and es; obsolete keys (`complete.reasonPlaceholder`,
  `complete.confirmNotWorked`) are removed from all three locales (grep: zero hits).

### Per-DEF verdicts

| DEF | Verdict | Evidence |
|---|---|---|
| DEF-01 | VERIFIED | `src/domain/overlap.ts` (overnight-aware interval math) + 10 unit tests; live non-blocking warning card in `shift-form.tsx` (`accessibilityRole="alert"`); OVERLAP badge in the agenda via `overlappingShiftIds` over a ±1-day window |
| DEF-02 | PARTIAL | Reason-code picker, `employer_cancelled` → `cancelled` (else `missed`), note required for `other`, `not_worked_note` column, localized labels — all correct. BUT the code set is `sick / no_show / employer_cancelled / schedule_error / other`, not PRD §9's `sick / employer_cancelled / personal / emergency / schedule_conflict / weather_or_transportation / other`. Decision needed: accept the reduced set or align with the PRD |
| DEF-03 | VERIFIED | CSV now 32 columns incl. `scheduled_breaks` / `actual_breaks` (serialized "Lunch 13:00 30min unpaid"), `other_income`, `deduction_rate` (%), `not_worked_note` |
| DEF-04 | VERIFIED | `money()` uses `Intl.NumberFormat(locale, {currency: 'CAD', narrowSymbol})` with per-locale cache + fallback. Note: CAD hardcoded as launch currency — ties to open PRD §21 decision #4 |
| DEF-05 | VERIFIED | Per-break editable start/duration when taken, "Add break" button, re-validated through `validateShiftWindow` before step 2 |
| DEF-06 | VERIFIED | "+1 day" cue shown under the end-time field in both `shift-form.tsx` and `complete/[id].tsx` whenever end ≤ start; localized in all three languages |
| DEF-07 | VERIFIED | "Deduction rate (%)" field at completion, prefilled snapshot → employer → global, clamped 0–100, snapshotted on save |
| DEF-08 | VERIFIED (claimed scope) | `employersRepo.remove` now throws `employer_has_shifts` before touching any row; shifts are never deleted. Employer default-rate field and edit/archive UI remain open backlog items (unchanged scope) |
| DEF-09 | VERIFIED | Week-nav labels use `tr('schedule.prevWeek'/'nextWeek')`; keys present in en/fr-CA/es |
| DEF-10 | VERIFIED | `roundCents` re-quantizes via `toFixed(6)`; probe: 1.005→1.01, 2.675→2.68, 123.375→123.38, 0.1+0.2→0.30, PRD examples unchanged; regression test in place |
| DEF-11 | PARTIAL | Employer mini-form shows inline localized errors for empty name / invalid rate and clears on edit. The role mini-form (`onAddRole`) still fails silently on invalid input — same class of issue, unfixed |
| DEF-12 | VERIFIED | "Mark disputed" chip overrides the derived status, persists via `payoutStatus`, rehydrates on reopen; `derivePayoutStatus` still never derives `disputed` |
| DEF-13 | VERIFIED | `savingRef` + disabled button in both `shift-form.tsx` and `complete/[id].tsx`; guard correctly released on validation failure and on save-and-add-another |
| DEF-14 | VERIFIED | `deduction_rate_bp` / `deduction_rate_snapshot_bp` INTEGER columns (0–10000), clamped `fractionToBp`/`bpToFraction` converters; snapshots round-trip exactly. Schema changed in place (unreleased DB, owner ruling documented in `db.ts`) — any pre-existing dev database must be wiped |
| DEF-15 | VERIFIED | `Chip` `minHeight` now `TOUCH_TARGET` (48) |

**Rollup: 13 VERIFIED · 2 PARTIAL (DEF-02 reason-code set vs PRD; DEF-11 role mini-form) · 0 NOT-FIXED.**

### Regressions

None found. All 98 tests pass, typecheck clean, no behavior change detected in the
untouched calculation paths (PRD §10 worked examples still exact). Two follow-ups,
not regressions: (1) `no_show` as an employee-side reason label reads oddly for a
self-tracking user — worth reviewing together with the DEF-02 code-set decision;
(2) negative half-cents round toward +∞ (`roundCents(-1.005) → -1.00`) — only
reachable through variance display, at most 1 cent, cosmetic.
